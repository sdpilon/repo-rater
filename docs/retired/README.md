# Retired

Plans for code that no longer exists in this repo — the old DuckDB-backed
`pipeline/` stack, fully deleted early in the project's history. Nothing
in this directory is runnable or current; safe to skip. See `docs/README.md`
for how this fits with the rest of `docs/`.
