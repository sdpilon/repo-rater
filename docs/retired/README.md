# Retired

Plans for code that no longer exists in this repo — the old DuckDB-backed
`pipeline/` stack, fully deleted as of Phase 4 (see `ARCHITECTURE.md`'s
"Original design (retired)" section). Nothing in this directory is
runnable or current; safe to skip.
